# Information Architecture

## Top-Level
- /
- /discover
- /search
- /destinations/[slug]
- /places/[slug]
- /stories/[slug]
- /people/[slug] — Phase 2
- /objects/[slug] — Phase 2
- /collections/[slug]
- /saved
- /trips — Phase 2
- /studio — Editorial

## Objektgraph
Destination
→ Places
→ Stories
→ People
→ Objects
→ Collections

Jedes Objekt darf andere Objekte referenzieren. Keine Sackgassen.

## URL-Prinzip
Lesbare, stabile Slugs. Keine Kategorieverschachtelung im Pfad, damit Inhalte später neu kuratiert werden können.
