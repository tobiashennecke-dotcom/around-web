import type {Metadata} from "next";import {redirect} from "next/navigation";import {getCommercialAccess} from "@/lib/partner-os/commercial-access";import LiveCalendar from "./live-calendar";export const dynamic="force-dynamic";export const metadata:Metadata={title:"Campaign Calendar / Private — AROUND",robots:{index:false,follow:false}};export default async function Page(){if(!await getCommercialAccess())redirect("/account");return <LiveCalendar/>;}