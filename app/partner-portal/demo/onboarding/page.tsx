import { Suspense } from "react";import { notFound } from "next/navigation";import type { Metadata } from "next";import OnboardingDemo from "./onboarding-demo";export const metadata:Metadata={title:"Your AROUND Onboarding — Preview",robots:{index:false,follow:false}};export default function Page(){if(process.env.VERCEL_ENV!=="preview")notFound();return <Suspense fallback={<main>AROUND lädt …</main>}><OnboardingDemo/></Suspense>;}